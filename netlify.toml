[build]
  command = "cd frontend && npm run build"
  publish = "frontend/dist"
  node_version = "20"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
